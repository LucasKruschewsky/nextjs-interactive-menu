import styled from "styled-components";

export const HeroContainer = styled.div`
  width: 100%;
  height: 60vh;

  position: relative;

  background: radial-gradient(
    ellipse,
    var(--backgroundColorHigh) 35%,
    var(--backgroundColor) 100%
  );
`;
