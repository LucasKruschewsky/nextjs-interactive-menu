import styled from "styled-components";

export const ComponentContainerWrapper = styled.div`
  max-width: 1440px;
  margin: auto;
  padding: 0 var(--spacingVeryLarge);

  div {
    .containerWrapper {
      width: 100%;
    }
  }
`;
